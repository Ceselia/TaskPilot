# 「一句话代办」运行设计说明（MVP v1）

> 读者目标：读完本文即可理解从一句话指令到任务完成的完整运行过程，以及用户在每个节点需要承担的动作。

## 1. 总体流程

```
① 接收指令
② 意图澄清（仅当模糊时）
③ 计划生成
④ 计划预览（用户确认）
⑤ 安全准备
⑥ 分步执行（每步留存执行记录 + 完成断言）
⑦ 异常处理（重试 → 降级 → 安全中止）
⑧ 结果汇报
⑨ 用户验收与反馈
```

运行原则：**入口控一次（计划），出口控一次（验收/发送闸门），中间只在危险动作和真异常时打扰用户。**

### 1.1 全程实时展示（思维链式流式输出）

**运行在任意一个阶段，助手都要向用户实时展示"当前进度"与"接下来准备做什么"**，以思维链（CoT）流式输出的方式呈现，杜绝黑盒执行：

| 阶段 | 实时展示内容 |
|---|---|
| ② 意图澄清 | 流式展示理解过程："我理解你要……，其中'张三'有 2 个候选，需要你确认" |
| ③ 计划生成 | 流式输出规划思路："我先扫描下载目录 → 再提取字段 → ……"，让用户看到计划是如何被推导出来的 |
| ⑤ 安全准备 | "正在备份 12 个文件到隔离目录……已完成 12/12" |
| ⑥ 分步执行 | 当前步骤高亮 + 已完成步骤打勾 + 待执行步骤置灰；每步开始前一句话预告（"接下来我将在邮件中新建草稿"），完成后一句话小结（"已找到 12 个发票文件"）；关键动作附带结果截图 |
| ⑦ 异常处理 | 流式说明故障定位过程："步骤 3 断言失败：预期 12 个文件，实际 11 个。正在重试……" |
| ⑧ 结果汇报 | 一句话摘要 + 全程执行记录时间轴回放 |

展示通道为常驻的进度面板（流式文本 + 步骤状态），用户无需主动询问即可掌握全部过程；面板同时提供暂停/取消入口。

## 2. 各阶段设计

### ① 接收指令

用户在助手界面输入（或语音转写）一句自然语言，例如：

> "把下载目录里的发票整理一下，汇总个表发给张三。"

助手不做任何执行动作，先进入理解阶段。

### ② 意图澄清（N1）

**触发条件（满足其一即触发，否则跳过）**：

- 指代模糊：如"那些发票""发给同事"——**P0 不访问通讯录**，直接请用户给出完整收件人；
- 范围模糊：如"整理一下"无法确定是归档还是删除（删除类会直接拒绝）；
- 目标模糊：缺少明确的完成产物。

**交互方式（P0：仅单轮聚合澄清）**：

一次性反问，聚合所有不确定点，不连环追问：

> "需要确认两个信息：① 收件人'张三'的完整邮箱是？（我暂不访问通讯录）② '整理'是指把发票按月份归档并重命名吗？"

用户回一句即可推进。

> 开放式指令（"帮我做个市场分析"）的多轮澄清收敛为 **P2 能力**：P0 阶段此类指令直接拒绝并说明原因与建议（见《能力说明》4.3）。

### ③ 计划生成

助手生成结构化执行计划，每个步骤包含：

| 字段 | 说明 |
|---|---|
| 步骤描述 | 用户可读的一句话 |
| 涉及软件 | 文件系统 / 邮件 / 表格软件等 |
| 输入 | 依赖上一步的哪个产物 |
| 前置条件 | 执行前必须成立的条件（如"目录中存在 ≥1 个发票文件"） |
| 完成断言 | 执行后必须成立的可验证条件（如"归档目录中存在 12 个重命名文件"） |
| 失败预案 | 重试 / 降级 / 中止 |

**完成断言是本设计的核心**：它与"盲目跑完步骤"的本质区别在于，每一步执行后立即机器可验证，是"结果可验证性"的来源。

### ④ 计划预览（N2）

向用户展示完整计划（步骤列表 + 关键产物预览，如解析出的收件人、将生成的文件名清单）。

- 用户可以：直接确认 / 修改某一步 / 取消；
- **信任递进（P1 能力）**：同类任务连续 3 次成功后，N2 自动升级为免确认——P0 阶段一律要求确认，不做免确认升级；包含对外发送步骤的任务即使未来引入信任递进，也永远展示计划并要求确认。

### ⑤ 安全准备

- **纯只读任务**（扫描、汇总、展示）：跳过本阶段；
- **涉及写操作的任务**（移动、重命名、创建文件）：先将受影响的原始文件**备份至隔离目录**（如 `~/.ai-assistant/backup/<任务ID>/`），备份完成才允许进入执行；
- 备份动作本身也有完成断言（备份文件数 = 待操作文件数）。

### ⑥ 分步执行

- 按计划逐步执行，步骤间传递状态（上一步的产物作为下一步输入）；
- 每步执行完**立即运行完成断言**；
- **每一步留存执行记录**：操作描述、执行时间、结果截图、断言结果、产物路径；
- 用户可随时查看实时进度，可随时暂停；
- 涉及 UI 自动化时，通过 macOS Accessibility API / Apple Events 操作，操作前检查目标元素存在性。

### ⑦ 异常处理（N4）

断言失败或执行异常时，按序降级：

```
失败 → 重试 1 次（修正参数，如重试时先检查目标窗口状态）
     → 降级方案（如"邮件客户端无响应，改为生成 .eml 草稿文件由用户双击发送"）
     → 安全中止：停止后续步骤 + 汇报已完成/未完成清单 + 提示回滚选项
```

安全中止时原始数据完好（有隔离备份），最坏损失是时间。

### ⑧ 结果汇报

任务完成后输出：

- 一句话摘要（"已整理 12 张发票并归档，汇总表在 ~/Documents/发票汇总-2026-09.xlsx，邮件已发送给张三（市场部）"）；
- 产物入口：文件路径 / 已发送邮件位置 / 草稿位置；
- 完整执行记录入口：可逐步回放每步的操作与截图。

### ⑨ 用户验收与反馈（N5）

- 用户 10 秒内可完成验收（打开产物看一眼）；
- 反馈机制：👍/👎 + 可补充一句原因；
- 反馈与执行记录一并回流，用于能力迭代与边界复审。

## 3. 用户介入节点总表

| 节点 | 名称 | 触发条件 | 用户动作 | 能否跳过 |
|---|---|---|---|---|
| N1 | 意图澄清 | 指令含模糊指代/范围/目标 | 单轮补充一句话（含提供完整收件人） | 指令明确时自动跳过 |
| N2 | 计划确认 | 默认所有任务 | 看计划、可修改、确认执行 | P0 阶段一律确认（免确认信任递进为 P1 能力） |
| N3 | 发送闸门 | 对外发送动作执行前 | 逐次确认收件人、附件清单、正文 → 助手代发 | **永不跳过（v1 硬约束）** |
| N4 | 异常求助 | 重试 + 降级均失败 | 人工接管或放弃（此时已有备份） | — |
| N5 | 结果验收 | 任务完成 | 10 秒验收 + 👍/👎 反馈 | 可忽略，靠完成断言兜底 |

### N3 发送闸门细节（P0：仅确认后代发）

在邮件即将发送前，助手弹出确认界面，**逐项展示**：

- 收件人（用户在 N1 提供的完整邮箱，逐字展示以识别"发错人"风险）；
- 附件清单（文件名 + 数量）；
- 正文全文。

用户逐项核对后点击确认，**由助手执行发送**。确认只对本次发送有效，不产生"以后都允许发给这个人"的记忆。

> 用户自行修改草稿后自主发送（路径 B，含助手自动检测收尾）为 **P1 能力**，P0 阶段不提供。

## 4. 典型任务全流程示例

> 指令："把下载目录里的发票整理一下，汇总个表发给张三。"

| 步骤 | 助手动作 | 断言 | 用户介入 |
|---|---|---|---|
| N1 | 单轮澄清：询问收件人与整理方式 | 用户给出邮箱 + 确认按月归档 | 确认 zhangsan@market.com |
| 计划 | 生成 5 步计划 | — | N2 确认计划 |
| 备份 | 复制 12 个发票文件到隔离目录 | 备份数 = 12 | 无 |
| 步骤1 | 扫描下载目录，识别发票文件 | 找到 ≥1 个发票文件 | 无 |
| 步骤2 | 提取金额/日期/对方单位 | 提取字段完整率 ≥ 90% | 无 |
| 步骤3 | 归档重命名至 `~/Documents/发票/2026-09/` | 归档目录存在 12 个重命名文件 | 无 |
| 步骤4 | 生成汇总表 `发票汇总-2026-09.xlsx` | 文件存在且行数 = 12 | 无 |
| 步骤5 | 起草邮件（附件已挂） | 草稿存在，收件人 = 张三 | **N3 逐项确认后助手代发** |
| 汇报 | 一句话摘要 + 产物路径 + 执行记录 | — | N5 验收 + 反馈 |

全程用户实际操作：**2 次确认（计划 + 发送）+ 1 次澄清，总耗时 < 1 分钟**；助手执行耗时约 3~5 分钟。

## 5. 运行时安全约束（v1 硬约束汇总）

1. 删除原始文件、格式化、覆盖写入：计划层直接拒绝生成；
2. 对外发送（邮件）：必须经过 N3 闸门，用户逐次确认后由助手代发；闸门不可跳过、不可记忆化；
3. 任何写文件系统操作前必须有隔离备份且备份断言通过；
4. 所有步骤留痕（操作 + 截图 + 断言结果），事后可完整回放；
5. P0 不访问通讯录：收件人一律由用户在澄清时提供；
6. 遇到验证码/生物识别界面：立即停止并转入 N4 求助，不尝试绕过。
