# 「一句话代办」能力说明（MVP v1）

> 读者目标：读完本文即可判断「某个任务该不该交给助手 / 助手会不会承接」。

## 1. 能力定位

桌面端 AI 助手在 macOS 上提供跨软件的自动化任务执行能力：用户以一句自然语言下达任务，助手独立完成「理解 → 规划 → 执行 → 汇报」的完整流程。

与现有单步指令的本质区别：**编排权从用户转移到助手**。用户不再是流程的 CPU，不再需要在多个软件之间手动切换、搬运上下文。

**目标平台**：仅 macOS。

## 2. 用户痛点与要解决的问题

- 不是用户"不会操作软件"，而是**跨软件的上下文搬运成本**：找文件、切窗口、复制粘贴、核对、再切换。
- 单步指令把任务拆解、步骤编排、结果核对全部留给了用户。
- 核心风险（决定能力边界的三条红线）：
  1. **错误不可逆**——删错文件、发错人、付错款；
  2. **过程不可见**——用户不知道助手在干什么、干了什么；
  3. **失败不可恢复**——中途失败留下烂摊子。

## 3. 承接边界总览

用四个维度给任务打分：**结构化程度 / 错误可逆性 / 数据敏感度 / 软件可控性**。

| 维度 | P0 承接范围 | MVP 明确拒绝 |
|---|---|---|
| 任务结构 | 可映射到 P0 标准样板（见 4.2）的线性任务 | 无法映射到样板的任务：开放式无终止条件（P2 前直接拒绝）、需跨表格/浏览器的任务（P1 前） |
| 操作性质 | 只读、复制、移动、重命名、汇总、起草、**闸门确认后的对外发送** | 删除原始文件、覆盖、格式化；付费/下单类；上传敏感数据到外部网站；用户自主发送路径（P1 前） |
| 失败代价 | 失败 = 浪费几分钟，可回滚 | 失败 = 数据丢失 / 资金损失 / 名誉风险 |
| 软件范围 | **仅 2 款**：macOS 文件系统 + 邮件客户端 | 表格软件与浏览器（P1/P2）；需过验证码/人脸/动态令牌的界面；专有加密客户端；远程环境；通讯录（P0 不访问） |
| 任务规模 | ≤5 步、2 款软件、≤10 分钟 | 长时后台任务、跨数十款软件 |

## 4. MVP 能力边界（按优先级收缩）

> **MVP 没有必要做大做全。v1 只做优先级最高的 P0 能力：一类任务模板、两款软件、一条发送路径。其余能力分级延后。**

### 4.0 P0 / P1 / P2 分级总表

| 级别 | 定义 | 内容 |
|---|---|---|
| **P0（MVP 唯一承诺）** | 少量用户高频、失败安全、可完整验证 | 见 4.1 |
| P1（v1.x 迭代） | P0 跑通且验证达标后再做 | 用户自主修改后自主发送（路径 B）；通讯录授权与指代消解（P0 阶段直接在澄清时问用户要收件人）；表格软件（Excel/WPS）接入；N2 免确认信任递进；第 2~3 类任务模板（按类型归档、批量草拟多封草稿） |
| P2（v2+） | 依赖前两级的数据与能力沉淀 | 开放式指令多轮澄清收敛（"做个市场分析"类）；浏览器基础操作；长任务（>5 步） |

**P0 明确不做（即使技术上容易实现也不做，防止范围蔓延）**：路径 B 自主发送、通讯录访问、开放式指令多轮澄清、表格/浏览器软件、信任递进免确认、多套任务模板。

### 4.1 P0 能力清单（v1 一句话版本）

> **v1 只承诺一件事：macOS 上「本地文件整理 + 信息汇总 + 邮件草拟并经闸门确认后代发」这一类线性任务。**

具体约束：

| 维度 | P0 范围 |
|---|---|
| 任务类型 | **仅一类任务模板**：扫描指定目录 → 提取字段 → 备份后归档重命名 → 汇总成表 → 起草邮件 → 闸门确认后由助手发送 |
| 指令形态 | 结构化或轻度模糊指令；含不确定点时**单轮聚合澄清**（一次性问全，不追问） |
| 软件范围 | **仅 2 款**：macOS 文件系统 + 邮件客户端 |
| 操作性质 | 只读、复制、移动、重命名、汇总、起草、**闸门确认后的对外发送**；删除/覆盖/格式化/付费一律拒绝 |
| 发送路径 | **仅路径 A**：N3 闸门逐项展示（收件人/附件/正文）→ 用户确认 → 助手代发。闸门不可跳过、不可记忆化 |
| 任务规模 | ≤5 步、2 款软件、≤10 分钟 |
| 必备机制 | 写前隔离备份 + 每步完成断言 + 全程流式进度展示（当前进度 + 下一步预告）+ 执行留痕 |
| 收件人解析 | **不访问通讯录**；指令中收件人不明确时，在单轮澄清中直接请用户给出完整收件人 |

### 4.2 典型 P0 任务（唯一标准样板）

**整理发票并发送**：扫描下载目录中的发票文件 → 提取金额/日期/对方单位 → 备份原件 → 按月份归档重命名 → 汇总成表格 → 起草邮件（附件已挂）→ 用户在闸门确认 → 助手发送 → 一句话汇报。

P0 阶段所有承接判断都对照该样板：**能映射到该模板的任务承接，映射不到的明确拒绝**（拒绝话术说明"我目前只支持文件整理汇总并发邮件这一类任务"）。

### 4.3 开放式指令（P2，非 MVP）

"帮我做个市场分析"类开放式指令在 P0 阶段**直接拒绝并说明原因**（"我暂时无法处理没有明确完成产物的任务，你可以告诉我期望的最终文件，比如'整理这些文件并汇总成表发给我'"）。多轮澄清收敛机制列入 P2——先用 P0 验证"编排权转移"这一核心假设，再扩展指令理解范围。

### 4.4 边界的演进

每个版本根据使用数据（拒绝率、降级采纳率、用户反馈）复审：P0 验证达标（见《验证说明》）后按 P1 → P2 顺序扩展；拒绝率高的任务类型，反推是"能力不足需补齐"还是"应永久划出边界"，并同步更新本文档。

## 5. 优雅降级（拒绝 ≠ 死板报错）

对超界任务，助手不静默失败，而是给出可执行的降级建议：

> "这个任务整体超出了我目前的能力范围（原因：涉及付费操作）。我可以完成前 3 步：找到并整理这些文件、汇总成表格、起草邮件，最后的下单动作需要你手动完成。要继续吗？"

降级原则：**能承接的子链路照常承接，把不可承接的部分明确交还用户，并说清交接点。**

## 6. 用户愿意托付的判断依据

托付意愿可操作化为：

> **托付意愿 ≈ （任务频率 × 单次节省时间） × 结果可验证性 ÷ 失败代价**

四条可执行判据：

1. **能力可信**：同类任务的历史成功率可见；
2. **结果可验**：任务结束有可检查的产物（汇总表、归档目录、已发送邮件），用户 10 秒内可验收；
3. **过程可控**：执行前可见计划、执行中可暂停、事后可回滚；
4. **失败安全**：最坏情况只损失时间，不损失数据、资金与名誉。

**产品推论**：MVP 优先切"高频 × 耗时 × 产物明确 × 低风险"的任务（文件整理、信息汇总、草拟分发），而非炫技但高风险的任务。

## 7. 与现状的差距

| 环节 | 现状 | P0 需补齐 |
|---|---|---|
| 指令理解 | 单步、动词明确 | 针对样板任务的自然语言理解 + 单轮聚合澄清 |
| 规划 | 无 | 样板任务的固定计划模板 + 每步完成断言 |
| 执行 | 单软件单步 | 文件系统 + 邮件客户端（AppleScript-Apple Events）的原子操作 + 步骤间状态传递 |
| 失败处理 | 报错后用户自理 | 重试 / 安全中止 + 回滚 |
| 信任机制 | 无 | 计划确认（N2）、发送闸门（N3，仅路径 A）、执行留痕 |
| 进度展示 | 无 | 全程流式输出（当前进度 + 下一步预告） |

## 8. 边界的演进

不承接 ≠ 永久不做。每个版本根据使用数据（拒绝率、降级采纳率、用户反馈）复审边界：拒绝率高的任务类型，反推是"能力不足需补齐"还是"应永久划出边界"，并同步更新本文档。
