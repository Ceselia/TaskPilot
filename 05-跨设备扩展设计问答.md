# 扩展问答：覆盖硬件与软件版本差异更大的设备时，最先需要重新设计的模块

> 问题背景：MVP 在 macOS 单平台验证通过。当「一句话代办」能力需要覆盖硬件与软件版本差异更大的设备（不同操作系统、不同 macOS/Windows 版本、不同性能档位、不同邮件客户端与办公软件版本）时，最先需要重新设计的是哪一模块？

---

## 结论

> **最先需要重新设计的是「执行层」——跨软件原子操作库（含原子操作定义、平台驱动实现、完成断言的取数方式三部分），并将它从"平台绑定代码"重构为"能力抽象层 + 平台驱动适配"架构。**

## 当前架构中执行层的位置

MVP 的运行链路为：

```
意图理解（LLM） → 计划生成（步骤 + 断言声明） → 执行层（原子操作 + 断言取数） → 汇报
```

MVP 执行层的现状是**平台强绑定**的：

| 执行层组件 | MVP 现状 | 绑定点 |
|---|---|---|
| 文件原子操作 | 直接调用 macOS 文件系统语义 | 路径规则（~/Downloads）、文件类型/UTI 识别、归档与移动语义 |
| 邮件原子操作 | AppleScript / Apple Events 驱动单一邮件客户端 | 客户端专属脚本字典、草稿/发送的专有接口 |
| UI 感知与操作 | Accessibility API（AX 树） | 元素定位方式、事件模型均为 macOS 专有 |
| 完成断言取数 | 从上述专有接口读状态（数文件、查草稿收件人） | 断言能否"机器可验证"完全取决于该平台是否暴露可读状态 |

## 为什么是执行层最先重设计，而不是其他模块

### 1. 其他模块是平台无关的，执行层是唯一的平台绑定层

- **意图理解与计划生成**：工作在自然语言与"步骤/前置条件/断言"这一抽象层，不关心底下是 AppleScript 还是 Windows UIA；
- **流程与介入节点（N1~N5）**：澄清、计划确认、发送闸门、验收反馈是交互协议，与设备无关；
- **验证体系（A/B/C/D 分级与门槛）**：方法学跨平台通用，只需替换评测任务载体。

而执行层每换一个平台、一个软件版本，**所有 API 入口都变**。它是链路中唯一的"方言层"，跨设备差异全部汇聚于此。

### 2. 完成断言的可判定性是全流程可信的根基，而它由执行层决定

本方案与"盲目跑步骤"的本质区别在于每步完成断言。断言要成立，前提是**执行层能在该设备上取到机器可验证的状态**：

- macOS 邮件客户端可以通过脚本接口查到"草稿收件人 = X"；
- 换到不暴露脚本接口的客户端，或旧版本系统上 Accessibility 树结构不同，断言就退化为"看截图猜结果"——D 级（危险失败）风险直接上升，验证门槛（D = 0）无法守住。

因此执行层必须首先回答：**在差异设备上，"每一步可以被验证"这个前提如何继续成立。** 这不是适配工作量问题，而是方案根基问题。

### 3. 性能与硬件差异也在执行层被吸收

低端设备上 UI 自动化响应慢、超时策略、重试节奏、备份 IO 耗时，都改变执行层的调度与容错参数；上层规划逻辑对此无感。差异越大，执行层越需要一个统一的"能力探测 + 超时/降级配置"机制。

## 重新设计的方向：执行层的三层重构

```
┌─────────────────────────────────────────────┐
│ 能力描述层（平台无关）                         │
│  以统一语义定义原子操作：                      │
│  scan_dir / extract_fields / move_rename /   │
│  make_table / create_draft / send_mail       │
│  + 每个操作的可验证完成断言契约                │
├─────────────────────────────────────────────┤
│ 驱动适配层（每平台/每软件一实现）              │
│  macOS 驱动：AppleScript + AX API            │
│  Windows 驱动：UI Automation + MAPI          │
│  版本子驱动：macOS 14/15、Mail 新旧版…        │
│  每个驱动声明自己支持的操作与断言取数能力       │
├─────────────────────────────────────────────┤
│ 能力探测与降级层                              │
│  运行时探测设备实际能力 → 生成能力档案 →       │
│  计划生成时按能力档案裁剪计划；断言取不到时     │
│  降级为"截图 + 用户确认"，或直接拒绝该任务     │
└─────────────────────────────────────────────┘
```

关键设计变化：

1. **计划生成从"固定模板"变为"按设备能力档案生成"**：同一句"整理发票发给张三"，在全能力设备上是 5 步代发，在只能读不能写的设备上裁剪为 3 步 + 交接点给用户——优雅降级下沉为执行层能力；
2. **断言分两级**：机器可验证断言（首选，能力档案支持时）与人工确认断言（降级，仅在闸门类节点使用），且后者只允许出现在 N3 这类本就有人工确认的节点上，不稀释 A/B/C/D 验证体系的判定力；
3. **验证体系同步扩充**：评测集按"平台 × 软件版本 × 能力档案"矩阵分层，交付门槛不变（A ≥ 80%、D = 0），但每个矩阵格单独统计。

## 一句话总结

> 意图理解、流程设计、验证方法都是平台无关的"语义层"，唯独执行层是与每台设备短兵相接的"物理层"；跨设备差异首先摧毁的不是功能，而是"每一步可被机器验证"这个可信根基。因此最先重设计执行层——把它从平台绑定代码重构为"统一能力语义 + 平台驱动 + 能力探测降级"的三层结构，其余模块只做随之而来的小幅适配（计划生成读取能力档案、评测集按设备矩阵分层）。
