# 「一句话代办」可交互演示 Demo 方案 README

> 本 Demo 完整演示一次典型跨软件任务从指令下达到任务完成的全过程，用于验证《运行设计说明》中的流程设计与介入节点（N1~N5）。

---

## 典型案例（本 Demo 演示的任务）

> **用户指令**："把下载目录里的发票整理一下，汇总个表发给张三。"

**任务定义**：助手扫描虚拟下载目录中的 12 张发票 → 提取金额/日期/对方单位 → 备份原件 → 按月份归档重命名 → 汇总生成 Excel 表格 → 通过虚拟邮件客户端起草邮件（附件已挂）→ 用户在发送闸门逐项确认 → 助手发送并汇报结果。

**该案例覆盖的介入节点**：

| 节点 | 环节 | Demo 中的呈现 |
|---|---|---|
| N1 | 意图澄清 | 单轮聚合澄清：直接询问收件人（P0 不访问通讯录）与整理方式 |
| N2 | 计划确认 | 5 步计划预览，用户可展开查看每步断言 |
| N3 | 发送闸门 | **仅路径 A**：逐项展示收件人/附件/正文 → 用户确认 → 助手代发 |
| N4 | 异常求助 | Demo 仅顺利路径，N4 机制见文末说明 |
| N5 | 结果验收 | 一句话摘要 + 产物入口 + 👍/👎 反馈 |

---

## MVP（P0）能力边界

> 本 Demo 严格对齐产品 MVP（P0）边界（详见《能力说明》4.0~4.2）：**只做优先级最高的一类任务模板，其余能力全部延后分级实现。**

| 维度 | P0（本 Demo 实现） |
|---|---|
| 任务类型 | **仅一类**：扫描目录 → 提取字段 → 备份归档 → 汇总成表 → 起草邮件 → 闸门确认后代发 |
| 软件范围 | **仅 2 款**：虚拟文件系统 + 虚拟邮件客户端 |
| 指令形态 | 结构化/轻度模糊；**单轮聚合澄清**（一次性问全，不追问） |
| 收件人解析 | **不访问通讯录**，由用户在澄清时提供完整邮箱 |
| 发送路径 | **仅路径 A**：N3 闸门逐项展示 → 用户确认 → 助手代发 |
| 任务规模 | 5 步、2 款软件、分钟级 |
| 必备机制 | 写前隔离备份 + 每步完成断言 + 全程流式进度（当前进度 + 下一步预告）+ 执行留痕 |
| 异常处理 | 仅顺利路径；引擎预留 `inject_failure()` 接口（P1 演示用） |

**P0 明确不做**：路径 B 自主发送、通讯录访问、开放式指令多轮澄清、表格/浏览器软件、信任递进免确认、多套任务模板、对抗指令演示——全部归入下文 P1/P2 路线图。

---

## 技术方案

| 项 | 选型 | 说明 |
|---|---|---|
| 后端 | Python 3.10+ / FastAPI / uvicorn | 单进程，内存态状态机 |
| 前端 | 原生 HTML + CSS + JavaScript | 零构建依赖，无 npm/webpack |
| 通信 | WebSocket（流式事件） + REST（用户动作） | 思维链流式输出走 WebSocket |
| 启动 | `python run.py` | 自动打开系统默认浏览器 |
| 外部依赖 | 仅 `fastapi`、`uvicorn` | 零网络依赖（无 LLM API、无 CDN），离线可演示 |

**LLM 模拟方式**：意图理解、计划生成、思维链文案均由后端预置的状态机 + 文案库驱动，每步输出内容固定，保证演示 100% 可复现。

---

## 目录结构

```
demo/
├── run.py                  # 启动入口：启动 uvicorn 并自动打开浏览器
├── requirements.txt        # fastapi + uvicorn
├── server/
│   ├── main.py             # FastAPI 应用、WebSocket 端点
│   ├── engine.py           # 任务状态机（①~⑨ 阶段流转、断言校验）
│   ├── scripts.py          # 预置剧本文案库（思维链流式输出内容）
│   └── state.py            # 虚拟文件系统 + 虚拟邮件的状态
├── data/
│   └── invoices/           # 12 个结构化模拟发票 .json（生成脚本预置）
│       ├── invoice_001.json   # { "对方单位": "…", "金额": 1200.00, "日期": "2026-08-03", ... }
│       └── …
└── web/
    ├── index.html          # 三栏同屏布局
    ├── style.css
    └── app.js              # WebSocket 订阅、面板渲染、用户交互
```

---

## 页面布局（三栏同屏）

```
┌────────────────────────────────────────────────────────────────┐
│  顶部：任务标题 + 流程阶段指示器（①接收 ②澄清 ③计划 … ⑨验收）      │
├──────────────┬──────────────────┬──────────────────────────────┤
│ 左栏：助手     │ 中栏：虚拟文件系统  │ 右栏：虚拟邮件客户端            │
│              │                  │                              │
│ · 对话消息流   │ · 下载目录视图     │ · 文件夹切换（收件箱/草稿/已发送）│
│   （思维链式   │   （12 张发票，   │ · 邮件列表                     │
│    流式输出）  │   归档时实时移动   │ · 草稿详情查看                  │
│ · 步骤进度面板 │   动画）          │ · 发送后移入"已发送"            │
│   （✓完成/    │ · 归档目录视图     │                              │
│    ▶进行/     │ · 隔离备份目录    │                              │
│    ·待执行）   │ · 产物：汇总表    │                              │
│ · 暂停/加速    │                  │                              │
│   控制按钮     │                  │                              │
└──────────────┴──────────────────┴──────────────────────────────┘
```

三栏同屏让观察者**同时**看到三个"软件"随任务推进发生的变化——助手在执行第 3 步时，中栏发票文件正在实时移动归档——这是"跨软件编排"最直观的呈现方式。

---

## 完整演示流程（剧本时序）

### 阶段 ① 接收指令

用户在左栏输入框输入（或点击预置的典型案例快捷按钮）：

> 把下载目录里的发票整理一下，汇总个表发给张三。

### 阶段 ② 意图澄清（N1，单轮聚合）

1. 助手流式输出理解过程：
   > "我理解你的任务是：整理下载目录中的发票 → 汇总成表 → 发送给'张三'。有两个信息需要你确认……"
2. 触发 N1 单轮聚合澄清（P0 不访问通讯录，直接询问）：
   > "请确认两个信息：① 收件人'张三'的完整邮箱是？（我暂不访问通讯录）② '整理'是指按月份归档并重命名吗？"
3. 用户回复（预置快捷回复按钮：`zhangsan@market.com，按月份归档`）。

> 说明：多轮澄清收敛开放式指令、通讯录授权消解均为 P1/P2 能力，不在本 Demo 范围（见《能力说明》4.0）。

### 阶段 ③ 计划生成

助手流式输出规划思路（思维链可见）：

> "根据你的确认，任务收敛为 5 个步骤：先扫描下载目录确认发票数量 → 提取关键字段 → 备份原件后按月份归档 → 生成汇总表 → 起草邮件并发送……"

### 阶段 ④ 计划预览（N2）

展示结构化计划卡片，每步可展开查看**前置条件 / 完成断言 / 失败预案**：

| # | 步骤 | 完成断言（示例） |
|---|---|---|
| 1 | 扫描下载目录，识别发票文件 | 找到 ≥1 个发票文件 |
| 2 | 提取金额/日期/对方单位 | 字段完整率 ≥ 90% |
| 3 | 备份原件 → 按月份归档重命名 | 归档目录存在 12 个重命名文件，备份数 = 12 |
| 4 | 生成汇总表 `发票汇总-2026-09.xlsx` | 文件存在且行数 = 12 |
| 5 | 起草邮件（附件已挂）→ 闸门 → 发送 | 草稿收件人 = zhangsan@market.com |

用户点击 **[确认执行]**。

### 阶段 ⑤ 安全准备

流式输出："正在备份 12 个文件到隔离目录 ~/.ai-assistant/backup/TASK-2026-0909/ …… 已完成 12/12"。中栏出现"隔离备份"目录并填充。

### 阶段 ⑥ 分步执行（流式自动 + 可暂停）

每步 1~3 秒推进，左栏步骤面板实时更新状态，中/右栏联动变化：

- **步骤 1**：中栏下载目录高亮扫描 12 个文件 → 流式小结"已找到 12 个发票文件"；
- **步骤 2**：流式输出提取表格（金额/日期/对方单位逐行流出）；
- **步骤 3**：中栏文件**带动画移动**至 `发票/2026-08/`、`发票/2026-09/` 归档目录并重命名（`2026-08-03_云启科技_1200元.json`）；
- **步骤 4**：中栏生成汇总表产物图标，点击可预览表格内容；
- **步骤 5**：右栏邮件客户端出现新草稿（收件人已填、附件已挂 12 项、正文含汇总统计）。

每步完成后左栏显示 ✓，并一句话预告下一步（"接下来我将在邮件中完成草稿…"）。

**控制**：面板提供 [暂停] [继续] [加速×3] 按钮。

### 阶段 ⑦ 发送闸门（N3，仅路径 A：确认后代发）

步骤 5 起草完成后，任务暂停在闸门，左栏弹出闸门卡片**逐项展示**：

- 收件人：张三 zhangsan@market.com
- 附件清单：12 个文件（名称列表）
- 正文全文预览

用户点 [确认发送] → 助手流式输出"正在发送……" → 右栏邮件从草稿移入"已发送"文件夹 → 助手继续收尾汇报。

> 闸门不可跳过、不可记忆化，这是 v1 硬约束。用户自主修改草稿后自主发送（路径 B）为 P1 能力，不在本 Demo 范围。

### 阶段 ⑧ 结果汇报

左栏输出一句话摘要 + 产物入口：

> "已整理 12 张发票并归档至 ~/Documents/发票/，汇总表在 ~/Documents/发票汇总-2026-09.xlsx，邮件已发送给张三（zhangsan@market.com）。"
>
> [查看汇总表] [查看已发送邮件] [查看执行记录]

### 阶段 ⑨ 用户验收与反馈（N5）

摘要下方出现 [👍 满意] [👎 不满意] 按钮，点击后 Demo 结束画面显示本次任务统计（总耗时、步骤数、用户干预 3 次：N1 澄清 + N2 计划 + N3 闸门）。

---

## 启动方式

```bash
cd demo
pip install -r requirements.txt   # 仅 fastapi + uvicorn
python run.py                     # 启动服务并自动打开浏览器 http://127.0.0.1:8000
```

- 无需配置任何 API Key、无需联网；
- 每次刷新页面重置剧本状态，可无限次重复演示；
- `python run.py --port 8001` 可指定端口；`--no-browser` 不自动开浏览器。

---

## 关键实现设计

### 1. 状态机引擎（server/engine.py）

```
RECEIVED → CLARIFYING(N1) → AWAIT_ANSWER → PLANNING
→ PLAN_REVIEW(N2) → BACKUP → STEP_1..5(断言校验) → GATE(N3)
→ SENDING(确认后代发) → REPORTING → FEEDBACK(N5) → DONE
```

每个状态转换通过 WebSocket 推送流式事件（`thought`（思维链文本片段）、`step_status`、`fs_change`、`mail_change`、`gate_open` 等），前端按事件类型驱动三栏联动渲染。

### 2. 断言校验（即使演示也不省略）

引擎在每个步骤结束时对虚拟状态跑真实断言（如归档后实际遍历归档目录计数 == 12），断言结果写入该步骤的执行记录。这保证 Demo 呈现的机制与《运行设计说明》一致，而非纯动画。

### 3. 思维链流式输出

文案按 30~80ms/字符 的间隔推送 `thought` 事件，前端逐字渲染，配合 [加速×3] 可缩短等待；[暂停] 挂起事件推送，恢复后继续——演示节奏完全可控。

---

## P1 / P2 能力路线图（本 Demo 不实现，交互细节已预设计）

以下能力均不在 P0 范围（见《能力说明》4.0），但设计细节在此保留，进入对应版本时直接按此实现，Demo 侧的扩展点也已标注。

### P1（v1.x 迭代）

**1. 路径 B：用户自主修改后自主发送**
- 闸门弹窗增加第二个按钮 [我要自己改]，与 [确认发送]（路径 A）并列；
- 点击后助手进入等待状态，流式输出"草稿已就绪，你可以在右侧邮件中修改，我会在你发送后自动继续"；
- 右栏内嵌邮件编辑器变为可编辑（收件人/附件/正文），用户修改后点邮件客户端自己的 [发送] 按钮；
- **自动检测收尾**：引擎在 `WAIT_USER_SEND` 状态轮询草稿状态，检测到草稿移入"已发送"即流式输出"检测到草稿已发送，继续收尾步骤"——等价于真实产品中检测邮件客户端草稿状态变化，用户无需回助手面板做任何操作；
- 状态机扩展：`GATE(N3) → [路径A: SENDING | 路径B: WAIT_USER_SEND → 检测已发送] → REPORTING`。
- 闸门约束不变：两条路径均必须经过闸门展示，不可跳过、不可记忆化。

**2. 通讯录授权与指代消解**
- 澄清前先弹出**模拟 macOS 系统授权弹窗**："AI 助手"想要访问你的通讯录 —— [不允许] [允许]；
- 允许 → 结合上下文做指代消解，唯一匹配直接填入并在计划预览中展示；多候选则进入澄清；
- 不允许 → 降级为直接询问完整收件人邮箱（即当前 P0 行为）；
- 预置剧本建议：通讯录中放"张三（市场部）zhangsan@market"与"张三（财务部）zhangsan@finance"两个联系人，演示歧义消解；
- 硬约束：未授权时无任何静默访问，越权即 D 级。

**3. N4 异常链路演示（故障注入开关）**
- 引擎已预留 `inject_failure()` 接口与断言失败分支；
- 开启开关后，归档步骤断言失败（预期 12 个实际 11 个），演示完整异常链路：流式输出故障定位（"步骤 3 断言失败：预期 12，实际 11，正在重试……"）→ 重试 1 次 → 降级方案 → 仍失败则 N4 求助卡片（人工接管或放弃，此时已有备份）。

**4. 对抗指令拒绝机制**
- 输入框实现指令匹配与拒绝话术："永久删除并清空废纸篓" → 拒绝或降级为安全子链路；
- "以后都不用确认了" → 闸门仍然触发（不可记忆化验证）。

**5. 其他 P1 项**
- 表格软件（Excel/WPS）接入（中栏增加第三个"软件"视图）；
- N2 免确认信任递进（同类任务连续 3 次成功后升级，含发送步骤的任务除外）；
- 第 2~3 类任务模板（按类型归档、批量草拟多封草稿）。

### P2（v2+）

**1. 开放式指令多轮澄清收敛**
- "帮我做个市场分析"类指令不再直接拒绝，进入多轮澄清：每轮聚合提问、流式展示收敛过程（"根据你刚才的补充，任务目前收敛为……，还差一个终止条件"），**上限 3 轮**；
- 收敛成功（"收集 3 份行业报告核心数据，与上季度汇总表对比，生成 10 页以内对比报告保存到指定路径"）→ 转正常承接；
- 3 轮后仍无明确完成判定 → 明确拒绝并给出建议（引导说出期望产物）；
- Demo 侧扩展：预置开放式指令剧本 + 收敛成功/失败双结局。

**2. 其他 P2 项**
- 浏览器基础操作（右栏之后增加第四视图或标签页）；
- 长任务（>5 步、跨 3 款软件）；
- 真实发票 OCR 模式：`data/invoices/` 替换为真实文件 + 接入解析器，引擎流程不变。

### 各版本 Demo 的验证衔接

P1/P2 能力进入实现时，同步扩充《验证说明》的评测任务集（如路径 B case、授权降级 case、多轮澄清收敛 case 与对抗 case），沿用同一套 A/B/C/D 分级与交付门槛，不做单独的验证体系。
